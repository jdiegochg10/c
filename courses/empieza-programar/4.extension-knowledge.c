#include <stdio.h>
#include <math.h>

int main ()
{
	int x, y;
	x = 10;
	y = 2;

	//operaciones basicas;

	x = x + y;	
	printf("El valor de x + y es: %i .\n", x);

	x = x - y;
	printf("El valor de x - y es: %i .\n", x);

	x = x * y;
	printf("El valor de x * y es: %i .\n", x);

	x = x / y;
	printf("El valor de x / y es: %i .\n", x);

	x = x % y;
	printf("El valor de x y es: %i .\n", x);

	x = pow(y,x);
	printf("El valor de x ^ y es: %i .\n", x);

	x = sqrt(x);
	printf("El valor de rai de  x: %i. \n", x );

	return 0;


}
